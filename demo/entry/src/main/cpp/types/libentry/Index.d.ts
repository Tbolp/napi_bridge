import { Size } from "@kit.ArkUI";

export const add: (a: number, b: number) => number;

export const area: (s: Size) => number;

export const square: (w: number) => Size;

export const str_add: (a: string, b: number) => string;
